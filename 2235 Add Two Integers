# Easy 
# 2235 Add Two Integers

# Base Code
class Solution(object):
    def sum(self, num1, num2):
        """
        :type num1: int
        :type num2: int
        :rtype: int
        """

# Solution
class Solution(object):
    def sum(self, num1: int, num2: int) -> int:
        """
        Time Complexity: O(1), Constant
        ** addition operation takes constant time
        ** returning takes constant time
            constant time means that the time it takes to perform the operation doesn't change in any input, which means  it always takes the same amount of time to complete
            adding any two numbers within a fixed size takes the same amount of time
        """
        return num1 + num2 